# Normalize line endings on commit; check out with LF everywhere.
* text=auto eol=lf

# The Windows launcher must keep CRLF to run correctly.
*.bat text eol=crlf

# Never touch binary artefacts.
*.jar binary
